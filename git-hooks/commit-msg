#!/usr/bin/env bash
set -euo pipefail

MSG_FILE="${1:?caminho da mensagem de commit}"
HOOK_DIR="$(cd "$(dirname "$0")" && pwd)"
# shellcheck source=validate-commit-message.sh
source "$HOOK_DIR/validate-commit-message.sh"

validate_commit_message_file "$MSG_FILE" "commit"
