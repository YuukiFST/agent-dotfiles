# Skills deleted from this repo that must also be deleted from live harness dirs.
#
# sync-config copies skills one directory at a time and never mirrors, on purpose, so a
# skill that exists only locally (plugin-provided, symlinked) survives a sync. The cost is
# that a skill *deleted* from skills/ also survives, on every machine that ever synced it.
# This list is the only thing that reaches those machines.
#
# One skill name per line. Everything after # is a comment. Drop a name once every machine
# has synced past its removal.

claude-md-auditor
effect
prove
agent-browser
loop-me
autoreview
wayfinder

# stacks/frontend deleted in #78 (replaced by ui-craft). A machine that once ran
# `stack.sh enable frontend` still holds these; the stack prune no longer reaches them.
animate
animation-vocabulary
apple-design
better-accessibility
better-colors
better-interface
better-layout
better-typography
better-ui
better-writing
brandkit
design-taste-frontend
emil-design-eng
find-animation-opportunities
gpt-taste
high-end-visual-design
image-to-code
imagegen-frontend-mobile
imagegen-frontend-web
impeccable
improve-animations
industrial-brutalist-ui
interface-review
minimalist-ui
pick-ui-library
react-doctor
redesign-existing-projects
review-animations
tailwind-v4-shadcn
transitions-dev
transitions-polish
