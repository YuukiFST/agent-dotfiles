// URL validation. Returns true if the URL is valid for shortening.

export function isValidUrl(url: string): boolean {
  // TODO: implement
  // Must be non-empty, non-whitespace, http or https scheme
  // Reject javascript: and other schemes
  throw new Error("NotImplementedError");
}
